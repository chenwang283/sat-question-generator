# Python dependencies for the SAT question generator skill.
#
# Install once per environment (Claude Code / Linux sandboxes often need the flag):
#   pip install -r requirements.txt --break-system-packages
#
# Used by:
#   scripts/check_math.py   -> sympy        (verifies Math answer keys)
#   scripts/build_docx.py   -> python-docx  (builds the final Word document)

sympy>=1.12
python-docx>=1.1.0
